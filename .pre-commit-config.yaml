repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.15.6
  hooks:
    - id: ruff-check
      args: [ --fix ]
      exclude: ^Ubuntu/keyboard/autokey/data/
    - id: ruff-format
      exclude: ^Ubuntu/keyboard/autokey/data/
